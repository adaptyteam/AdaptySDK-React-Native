// Mock credentials (no real API needed in mock mode)
const MOCK_TOKEN = 'mock_api_key';
const MOCK_PLACEMENT_ID = 'mock_placement';

// This function is only for this example
export function readCredentials(): string {
  return MOCK_TOKEN;
}

// This function is only for this example
export function readPlacementId(): string {
  return MOCK_PLACEMENT_ID;
}

